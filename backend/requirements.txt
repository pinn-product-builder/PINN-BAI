fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx>=0.27.0,<0.28.0
python-dotenv==1.0.1
supabase==2.10.0
apscheduler==3.10.4
pytest==8.3.4
pytest-asyncio==0.25.0
eval-type-backport>=0.2.0
# Email Outreach — criptografia simétrica de tokens OAuth e senhas SMTP.
cryptography>=42.0.0,<46.0.0
# Validação de EmailStr (pydantic v2) usada nos DTOs do Email Outreach.
email-validator>=2.0.0,<3.0.0
# Observability — Sentry SDK. Ativa só quando SENTRY_DSN está no env;
# sem DSN o backend roda normal e o pacote não é importado.
sentry-sdk[fastapi]>=2.0.0,<3.0.0
